import { SearchProgramProfilePipe } from './search-program-profile.pipe';

describe('SearchProgramProfilePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProgramProfilePipe();
    expect(pipe).toBeTruthy();
  });
});
