import { StatusCoursePipe } from './status-course.pipe';

describe('StatusCoursePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusCoursePipe();
    expect(pipe).toBeTruthy();
  });
});
