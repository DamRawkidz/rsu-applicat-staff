import { TotalScoreConfirmPipe } from './total-score-confirm.pipe';

describe('TotalScoreConfirmPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalScoreConfirmPipe();
    expect(pipe).toBeTruthy();
  });
});
