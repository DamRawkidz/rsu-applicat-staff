import { SearchProgramPipe } from './search-program.pipe';

describe('SearchProgramPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProgramPipe();
    expect(pipe).toBeTruthy();
  });
});
