import { ShowTablePipe } from './show-table.pipe';

describe('ShowTablePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowTablePipe();
    expect(pipe).toBeTruthy();
  });
});
